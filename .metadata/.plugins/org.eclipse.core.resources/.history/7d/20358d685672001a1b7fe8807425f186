package CodingStudyHW;
/*
 * Kruscal 복습
 *	
 * Kruscal -> MST 만들기
 * MST? -> 최소 신장 트리
 * 이를 위해 Union - Find 개념을 쓴다.
 * 자료구조는 간선리스트를 이용 
 * 
 * Step 1. 비용을 기준으로 가장 최소의 값부터 조회할 수 있도록 정렬
 * Step 2. Union-Find로 묶어준다. -> 부모가 같지 않은 경우에 묶어줌.
 * 
 */
public class 별자리만들기 {
	static class Star{
		int number;
		int x;
		int y;
		int from;
		int to;
		public Star(int number, int x, int y, int from, int to) {
			this.number = number;
			this.x = x;
			this.y = y;
			this.from = from;
			this.to = to;
		}
		
	}
	public static void main(String[] args) {
		
	}
}
