package BAEKJOON;

import java.util.ArrayList;
import java.util.Scanner;

/*
 * Eratosthenes' sieve 
 * 음.. 소수 중에 2보다 큰 짝수는 있을 수 없음
 */
public class 소수구하기 {
	static ArrayList<Integer> EratosthenesList = new ArrayList<Integer>();
	static boolean flag;
	static StringBuilder sb = new StringBuilder();
	static int N,M;
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		M = sc.nextInt();
		N = sc.nextInt();
		makeEratosThenesList();
		for(int i=0;i<EratosthenesList.size();i++) {
			if(EratosthenesList.get(i)>=M && EratosthenesList.get(i)<=N){
				System.out.println(EratosthenesList.get(i));
			}
		}
	}
	private static void makeEratosThenesList() {
        for(int i=M;i<=N;i++) {
            flag = true;
            int value = (int) Math.sqrt(i);
            for(int j=value;j>=2;j--) {
                //자기보다 작은애들 나눴는데 0이 나왔으면 소수가 아님.
                if(i%j==0) {
                    flag = false; //실패
                    break;
                }
            }
            if(flag) EratosthenesList.add(i);
        } 
	}
}
