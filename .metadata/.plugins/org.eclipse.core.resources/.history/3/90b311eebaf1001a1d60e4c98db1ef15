package 탐색;

import java.util.Arrays;

public class 입국심사 {
	/*
	 */
	public static void main(String[] args) {
		long answer = solution(6,new int[] {7,10});
		System.out.println(answer);
	}
    public static long solution(int n, int[] times) {
        long answer = 0;
        
        Arrays.sort(times); //오름차순 정렬
        
        long startTime = 1;
        long endTime = Integer.toUnsignedLong(times[times.length-1])*Integer.toUnsignedLong(n);
        System.out.println(startTime);
        while(startTime<=endTime){
        	int peopleCount = getPeopleCount(times,endTime);
        	long mid = (startTime+endTime)/2;

        	if(peopleCount>=n){//심사해야하는 사람들 보다 많거나 같으면 계속 진행  
        		endTime = mid;
        	}else{//심사해야하는 사람들 보다 적으면 
        		startTime = mid;
        	}
        }
      
        return answer;
    }
	private static int getPeopleCount(int[] times, long endTime) {
		int peopleCount = 0;
		for(int time : times) peopleCount+=(endTime/time);
		return peopleCount;
	}
}
