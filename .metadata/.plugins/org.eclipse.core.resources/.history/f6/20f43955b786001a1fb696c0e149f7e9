package BAEKJOON;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.PriorityQueue;
import java.util.StringTokenizer;

/*
 * 14:25~
 */
public class 연료채우기 {
	static int N;//주유소 개수
	static boolean[] gasPostion = new boolean[1000001];
	static class GasStation implements Comparable<GasStation>{
		int dist;//dist
		int gas;//fuel
		public GasStation(int dist, int gas) {
			this.dist = dist;
			this.gas = gas;
		}
		@Override
		public int compareTo(GasStation o) {
			return Integer.compare(this.gas,o.gas);
		}
	}
	static int L,P;
	static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
	static StringTokenizer st;
	static PriorityQueue<Integer> gasStationlist;
	static int stop;
	public static void main(String[] args) throws NumberFormatException, IOException {
		setData();
		move();
	}
	private static void move() {
		int dist=1;
		while(true){
			
		}
	}
	private static void setData() throws NumberFormatException, IOException {
		N = Integer.parseInt(br.readLine());
		
		for(int i=0,dist,gas;i<N;i++){
			dist = Integer.parseInt(st.nextToken());
			gas = Integer.parseInt(st.nextToken());
			gasPostion[dist]=true;
		}
	}
}
