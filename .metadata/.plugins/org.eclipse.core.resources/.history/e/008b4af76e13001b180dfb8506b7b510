package 탐색;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.PriorityQueue;

public class IcyPerimeter {
	static int N;
	static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
	static char[][] machine;
	static boolean[][] visit;
	static PriorityQueue<IceCream> pq;
	static int size,circumference;
	static class IceCream implements Comparable<IceCream>{
		int size; //영역 크기
		int circumference; //둘레
		public IceCream(int size, int circumference) {
			this.size = size;
			this.circumference = circumference;
		}
		@Override
		public int compareTo(IceCream o) {
			if(this.size==o.size) {
				return Integer.compare(this.circumference,o.circumference);
			}
			return -Integer.compare(this.size,size);
		}
	}
	public static void main(String[] args) throws NumberFormatException, IOException {
		N = Integer.parseInt(br.readLine());
		machine= new char[N][N];
		for(int row=0;row<N;row++) machine[row] = br.readLine().toCharArray();
		visit = new boolean[N][N];
		dfsAll();
	}
	private static void dfsAll() {
		for(int row=0;row<N;row++) {
			for(int col=0;col<N;col++) {
				
			}
		}
	}
}
